{
  "scripts": {
    "kit": "rm -rf .git && git init && npm i inquirer@3.0.6 && rm -rf package.json && npm init && node init && rm -rf node_modules && rm -rf init.js && rm -rf package.completion.json && npm i && rm -rf README.md && git add . && git commit -a -m 'Initial commit' && git branch release"
  }
}